<div class="container">
  <div class="row">
    <div class ="col-md-4 mb-3">
      <div class="card">
        <div class="card-body">
          <h4 class="card-title">List of nearby restaurants</h4>
          <% @places_restaurants.each do |place| %>
            <li><%= place %></li>
          <% end %>
        </div>
      </div>
    </div>
    <div class ="col-md-4 mb-3">
      <div class="card">
        <div class="card-body">
          <h4 class="card-title">List of nearby convenience stores</h4>
          <% @places_convenience.each do |place| %>
            <li><%= place %></li>
          <% end %>
        </div>
      </div>
    </div>
    <div class ="col-md-4 mb-3">
      <div class="card">
        <div class="card-body">
          <h4 class="card-title">List of nearby bars</h4>
          <% @places_bars.each do |place| %>
            <li><%= place %></li>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>