<div class="container">
  <div class="card-flat">
    <div class="card-body">
      <div id="hero-carousel" class="carousel slide carousel-fade card" data-ride="carousel">
        <div class="carousel-caption d-none d-md-block"></div>
        <div class="carousel-inner">
          <% @places.each_with_index do |place,index| %>
            <% if index == 0 %>
              <div class="carousel-item active">
                <img class="d-block w-100" src="<%= place.image_uri %>" alt="<%= place.name %>">
                <div class="carousel-caption d-none d-md-block">
                  <h4><%= place.name %></h4>
                </div>
              </div>
            <%else%>
              <div class="carousel-item">
                <img class="d-block w-100" src="<%= place.image_uri %>" alt="<%= place.name %>">
                <div class="carousel-caption d-none d-md-block">
                  <h4><%= place.name %></h4>
                </div>
              </div>
            <% end %>
          <% end %>
        </div>
        <a class="carousel-control-prev" href="#hero-carousel" role="button" data-slide="prev">
          <span class="carousel-control-prev-icon" aria-hidden="true"></span>
          <span class="sr-only">Previous</span>
        </a>
        <a class="carousel-control-next" href="#hero-carousel" role="button" data-slide="next">
          <span class="carousel-control-next-icon" aria-hidden="true"></span>
          <span class="sr-only">Next</span>
        </a>
      </div>
      <div class="simple-space"></div>
      <div class="container">
        <div class="row">
          <div class="col">
            <div class="card border-info">
              <div class="card-header card-header-danger mx-auto">
                <h4 class="card-title">Newest Place</h4>
                <!-- <p class="category">Category subtitle</p> -->
              </div>
              <div class="card-body">
                <h4 class="card-title"></h4>
                <h6 class="card-subtitle mb-2 text-muted"></h6>
                <p class="card-text">
                  <div class="list-group">
                    <% p = Place.last %>

                      <a href="<%= place_path(p)%>" class="list-group-item list-group-item-action flex-column align-items-start">
                        <div class="d-flex w-100 justify-content-between">
                          <h5 class="mb-1"><%= p.name %></h5>
                        </div>
                        <p class="mb-1"><%= p.description %></p>
                      </a>
                    </div>
                  </p>
                </div>
              </div>
            </div>
            <div class="col">
              <div class="card border-info">
                <div class="card-header card-header-danger mx-auto">
                  <h4 class="card-title">Newest Event</h4>
                </div>
                <div class="card-body">
                  <p class="card-text">

                    <div class="card-body">
                      <h4 class="card-title"></h4>
                      <h6 class="card-subtitle mb-2 text-muted"></h6>
                      <p class="card-text">
                        <div class="list-group">
                          <% e = Event.last %>

                            <a href="<%= event_path(e)%>" class="list-group-item list-group-item-action flex-column align-items-start">
                              <div class="d-flex w-100 justify-content-between">
                                <h5 class="mb-1"><%= e.name %></h5>
                              </div>
                              <p class="mb-1"><%= e.description %></p>
                            </a>
                          </div>

                        </p>
                      </div>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
